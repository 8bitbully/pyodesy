from math import sqrt, sin, cos, tan, atan, atan2, pi

fi = pi / 180

sinf = lambda rad: sin(rad * fi)
cosf = lambda rad: cos(rad * fi)
tanf = lambda rad: tan(rad * fi)
atanf= lambda rad: atan(rad* fi)
atan2f= lambda rad1, rad2: atan2(rad1*fi, rad2*fi)